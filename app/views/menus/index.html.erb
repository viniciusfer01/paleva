<h2>Cardápios do Estabelecimento</h2>

<% if @menus.any? %>
  <ul>
    <%= @menus.each do |menu| %>
      <li><%= link_to menu.name, menu_path(menu) %></li>
    <% end %>
  </ul>
<% else %>
  <p>Não há cardápios registrados...</p>
<% end %>