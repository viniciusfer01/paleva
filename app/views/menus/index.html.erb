<h2>Cardápios do Estabelecimento</h2>

<li><%= link_to 'Novo Cardápio', new_menu_path %></li>

<% if @menus.any? %>
  <ul>
    <%= @menus.each do |menu| %>
      <li><%= link_to menu.name, menu %></li>
    <% end %>
  </ul>
<% else %>
  <p>Não há cardápios registrados...</p>
<% end %>